export const opts = {
  //Desabilita timestamp attr (updateAt, cratedAt)
  timestamps: false,
  //Desabilitar alterações nas tabelas para que o sequelize não passe os nomes para o plural
  freezeTableName: true,
}
